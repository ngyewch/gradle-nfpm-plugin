name: ${NAME}
arch: ${ARCH}
version: ${VERSION}
section: extra
priority: optional
maintainer: Nick Ng <ngyewch@gmail.com>
description: |
  Test

contents:
  - src: ${INSTALL_DIR}
    dst: /opt/test
    type: tree
    expand: true
